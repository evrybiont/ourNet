.member.three.columns.gn-c
  = link_to user_path(user), data: {push: true}  do
    = image_tag user.avatar.url(:thumb), class: "#{highlight(user)}"
  %div= link_to user.name, user_path(user), data: {push: true}
