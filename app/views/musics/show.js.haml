$('#sidebar').replaceWith("#{j (render 'layouts/box/sidebar')}");
$('.stuff').html("#{j (render 'musics/shared/show_content')}")
new Behavior.Music()
window.history.pushState('', '', "#{music_path}");
