.two.columns#sidebar
  = link_to 'Dashboard', root_path, class: "button twelve columns #{active('dashboard')}"
  = link_to 'ArtWork', root_path, class: "button twelve columns #{active('artwork')}"
  = link_to 'Parts', root_path, class: "button twelve columns #{active('parts')}"
  = link_to 'Tools', root_path, class: "button twelve columns #{active('tools')}"
  = link_to 'Articles', root_path, class: "button twelve columns #{active('articles')}"
  = link_to 'Events', root_path, class: "button twelve columns #{active('events')}"
  = link_to 'Music', music_index_path, remote: true, class: "button twelve columns #{active('music')}"
  = link_to 'Videos', videos_path, class: "button twelve columns #{active('videos')}"
  = link_to 'Photos', photos_path, class: "button twelve columns #{active('photos')}"
  = link_to 'Users', users_path, class: "button twelve columns #{active('users')}"
