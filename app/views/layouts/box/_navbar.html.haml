.container.navbar
  .row.gn-c
    %span Ukraine Custom Culture
  .row
    .two.columns.nav-logo-gn
      = link_to root_path, class: 'navbar-brand' do
        = image_tag 'wolf2.jpg', class: 'nav-logo'

    .ten.columns.nav-menu.nav-auto-align
      - if current_user
        = link_to "#{current_user.name} Sign Out", destroy_user_session_path, method: 'delete', class: 'button'
      - else
        = link_to 'Sign in with Facebook', user_omniauth_authorize_path(:facebook)
