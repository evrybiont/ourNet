.container.navbar
  .row
    .two.columns
      = link_to '#', class: 'navbar-brand' do
        = image_tag 'wolf2.jpg', class: 'nav-logo'

    .ten.columns.nav-menu.auto-align
      = link_to "#{current_user.name} Sign Out", destroy_user_session_path, method: 'delete', class: 'button button-primary'
